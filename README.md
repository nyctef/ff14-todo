An(other) experiment in writing a todo app where the todos reset after specific intervals.

in `web/`:

```
npx pnpm install --frozen-lockfile
npm run start
```

in `srv/`:

```
npx pnpm install --frozen-lockfile
npm run start
```
